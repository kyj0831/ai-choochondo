OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Dev/test only: set to 1 to bypass real OpenAI calls and use deterministic
# sample responses (query generation, evidence judgment, report writing).
MOCK_LLM=0
